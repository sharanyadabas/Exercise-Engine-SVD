FROM python:3.9.12

ENV CONTAINER_HOME=/var/www

ADD . $CONTAINER_HOME
WORKDIR $CONTAINER_HOME

RUN pip install -r $CONTAINER_HOME/requirements.txt

COPY init.json $CONTAINER_HOME/

CMD python ${CONTAINER_HOME}/app.py